{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -d frontend ]; then cd frontend && npm install; else npm install; fi",
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm run build; else npm run build; fi",
  "devCommand": "if [ -d frontend ]; then cd frontend && npm run dev; else npm run dev; fi"
}
